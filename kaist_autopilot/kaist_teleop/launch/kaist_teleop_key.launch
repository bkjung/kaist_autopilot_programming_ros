<launch>
  <arg name="model" default="$(env TURTLEBOT3_MODEL)" doc="model type [burger, waffle, waffle_pi]"/>
  <param name="model" value="$(arg model)"/>

  <!-- kaist_teleop_key already has its own built in velocity smoother -->
  <node pkg="kaist_teleop" type="kaist_teleop_key" name="kaist_teleop_keyboard"  output="screen">
  </node>
</launch>
